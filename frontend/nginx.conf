server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # All normal routes → return index.html (SPA fallback)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
